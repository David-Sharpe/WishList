%h1 Listing wishlists

%table
  %thead
    %tr
      %th User
      %th Count
      %th
      %th
      %th
      %th

  %tbody
    - @wishlists.each do |wishlist|
      %tr
        %td= wishlist.user.email
        %td= wishlist.count
        %td= link_to 'Show', wishlist
        %td
          = link_to 'Edit', edit_wishlist_path(wishlist) if current_user.id == wishlist.user_id
        %td
          = link_to 'Destroy', wishlist, :method => :delete, :data => { :confirm => 'Are you sure?' } if current_user.id == wishlist.user_id
        %td
          %table
            %tbody
              - wishlist.wishlist_items do |item|
                %tr
                  %td= item.name
